# StockSphere

A production-ready, mobile-first inventory dashboard that uses Google Sheets as its primary database.

## Setup

1.  Clone the repository.
2.  Install dependencies: `pip install -r requirements.txt`.
3.  Set up Google Cloud Service Account and download JSON.
4.  Configure `.streamlit/secrets.toml`.
5.  Run locally: `streamlit run app.py`.
